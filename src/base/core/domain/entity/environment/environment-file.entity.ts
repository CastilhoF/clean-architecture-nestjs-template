enum EnvironmentFileEntity {
  DEVELOPMENT_LOCAL = '.env.local',
  DEVELOPMENT_SERVER = '.env.development',
  TESTING = '.env.testing',
  PRODUCTION = '.env.production',
}

export default EnvironmentFileEntity;
